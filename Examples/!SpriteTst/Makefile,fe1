# Project:   SpriteTest

.SUFFIXES:    .c .o
CC            = cc
CCFlags       = -c -throwback -IC: -Ff
LinkFlags     = -o $@
SqueezeFlags  = -f -v

Files         = o.!RunImage
Libs          = DeskLib:o.DeskLib C:o.Stubs


!RunImage:    $(Files) 
              link $(LinkFlags) $(Files) $(Libs)
              squeeze $(SqueezeFlags) !RunImage

.c.o:;        $(CC) $(CCflags) $< -o $@
